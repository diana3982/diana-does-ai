# Columba backend dependencies
#   pip install -r requirements.txt
#
# Requires Python 3.10+ (python-dotenv's floor). Developed on 3.14.

anthropic>=0.122.0     # Claude API — conversation + quirk extraction
flask>=3.1.3           # REST API
flask-cors>=6.0.5      # lets the Vite dev server (:5173) call the API (:5000)
python-dotenv>=1.2.3   # loads ANTHROPIC_API_KEY from .env
