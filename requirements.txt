# Columba backend dependencies
#   pip install -r requirements.txt
#
# Requires Python 3.10+ (python-dotenv's floor). Developed on 3.14.
#
# Floors are the versions actually verified against the live API; the major
# caps are there because an unbounded ">=" will happily install the next
# breaking release on someone's fresh clone. Raise a cap deliberately, after
# running `COLUMBA_LIVE=1 pytest tests/test_live.py`.

anthropic>=1.4.0,<2      # Claude API — conversation + background analysis
flask>=3.1.3,<4          # REST API
flask-cors>=6.0.5,<7     # lets the Vite dev server (:5173) call the API (:5000)
python-dotenv>=1.2.3,<2  # loads ANTHROPIC_API_KEY from .env
